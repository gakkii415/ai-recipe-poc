# AI content pipeline v2

## 目的

大量生成を1つの長い会話へ詰め込まず、人の操作回数も増やさない。記事単位の追跡性を保ったまま、生成・審査・修正をまとめて並列実行する。

## 固定単位

| 工程 | 1タスクの件数 | 50件試験 |
|---|---:|---:|
| 生成 | 5件 | 10タスク |
| AIレビュー | 10件 | 5タスク |
| 修正 | 不合格のみ | 2タスク |
| 人間レビュー | 抜き取り＋要修正 | 別途実施 |

## 状態

`planned → generated → machine_validated → ai_reviewed → repair → validated → published`

`review_status` は人間による確認状態を表す。AIレビューを通過しても `unreviewed` のままにする。

## ブランチ所有

- 親だけが統合ブランチと `main` を更新する。
- 生成タスクは `agent/<batch-range>` の専用ブランチと、割り当てられた5ファイルだけを変更する。
- レビュー担当は原則コードを変更しない。
- 修正担当は `repair/<group>` の専用ブランチと、不合格ファイルだけを変更する。
- 同一ファイルを複数タスクへ割り当てない。

## 検査ゲート

1. Ruby検査: YAML、必須キー、ID・題名の重複、ファイル名、数値型、本文構造、v2バッチ最大5件。
2. 別AI検査: 材料と工程、分量、時間、火加減、完成判断、食品安全、テンプレ感。
3. 修正後にRuby検査を再実行。
4. 人間は無作為抽出とAIで要修正になった記事を優先確認。
5. 合格した統合コミットだけを `main` へ進める。

## 失敗時

- 失敗は記事ID単位で扱い、合格記事を再生成しない。
- 修正は最大2回を目安にし、解消しなければ `rejected` として公開対象から外す。
- 大量バッチ投入前に50件で合格率、修正率、重複率、所要時間、コストを測る。
